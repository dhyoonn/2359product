## 2026-07-03

### 완료한 작업

**제안서 프롬프트 지침 보강 (proposal.ts / proposal-logic.ts / proposal-final.ts)**
- 근거 자료 인용 시 출처(저자·저널·연도)뿐 아니라 원문 URL 링크까지 반드시 함께 명시하도록 지침 추가. 확인 불가한 자료는 인용 금지. HTML 논문 근거 박스에 `target="_blank"` 하이퍼링크로 노출
- 원료 국내 사용 가능 여부 검증 지침 추가 — 식품·건강기능식품은 식품안전나라 + 식약처 건강기능식품 공전(고시형/개별인정형), 화장품은 식약처 고시 「화장품 안전기준 등에 관한 규정」(1차 기준) + 대한화장품협회 화장품성분사전(보조 기준)으로 판단. 사용 불가·제한 원료는 명시하고 대체 원료 제안
- 원료 선택이 특정 원료로 반복 수렴되는 경향 개선 — 기전에 맞는 후보 원료를 웹 검색으로 최소 3개 이상 비교한 뒤 기전 적합성·근거 강도·차별성 기준으로 선택하도록 원칙 추가 (습관적/친숙함 기반 재사용 금지)
- 전문 열람 기능(클릭 시 원문 전체를 앱 내에 표시)은 검토 후 제외 — 스크래핑 안정성(페이월·차단)과 클릭당 추가 토큰 비용 대비 이점이 적어 URL 링크 방식으로 충분하다고 판단

### 현재 상태
- 세 프롬프트 파일 수정 완료, 실제 제안서 생성 결과로 검증은 아직 안 함 (다음 실사용 시 확인 필요)
- 세션 간 상태 저장이 없는 구조라 "이전 기획안에서 쓴 원료 이력"을 실제로 추적하지는 못함 — 이번 변경은 매 생성 시 습관적 기본값 선택을 줄이는 프롬프트 차원의 개선

### 다음 작업
- 다음 제안서 실사용 시 출처 링크·국내 사용 가능 여부·원료 다양성이 실제로 잘 반영되는지 확인
- 필요 시 최근 사용 원료 이력을 파일로 저장해 다음 요청 프롬프트에 첨부하는 이력 기반 회피 기능 검토 (요청 시에만 진행)

### 특이사항 / 결정 사항
- 화장품 원료 판단은 대한화장품협회 화장품성분사전보다 식약처 고시(사용금지·제한 원료 목록)를 1차 법적 기준으로 삼기로 결정 — 성분사전은 실무 조회용 보조 자료로 위치 설정

---

## 2026-07-02

### 완료한 작업

**응답 속도 개선 — 스트리밍 전환 + 프롬프트 캐싱**
- `page-flow`: 서버가 스트림을 열어놓고도 `finalMessage()`로 끝까지 기다렸다가 한 번에 응답하던 문제 발견 → 실시간 델타를 그대로 클라이언트에 전송하도록 수정. 완료 후 `---PAGEFLOW-FINAL---` 구분자 뒤에 후처리된 최종 HTML을 이어붙임
- `final-plan` 최초 생성: 기존 `client.messages.create()` 블로킹 호출 → 위와 동일한 스트리밍 + 구분자(`---FINALPLAN-RESULT---`) 방식으로 전환. 부수 효과로 "생성 중지" 버튼이 최초 생성에도 정상 연결됨(기존엔 AbortController 미연결로 무동작)
- `page.tsx` (page-flow, final-plan): 스트림을 읽으며 "○○자 생성됨" 진행 상황을 화면에 실시간 표시
- `page-flow`, `proposal`(로직/마케팅/최종/초기) 시스템 프롬프트에 `cache_control: { type: 'ephemeral' }` 추가 — 반복 요청(대화형 수정 등) 시 응답 시작 속도 개선
- `final-plan`의 프롬프트는 정적 지침과 사용자 데이터가 한 문자열에 섞여 있어 캐싱 미적용 (구조 재설계 필요, 추후 검토)

**폴더 정리**
- 루트에 있던 `제품 사양서 기본 양식.xlsx`, `최종기획안 양식.pptx` 중복 파일 삭제 (실제 앱은 `web/` 안의 동일 파일을 읽음 — 루트 복사본은 미사용 잔재였음)
- 레퍼런스 샘플 문서 7개 + 미추적 리뷰 원본 데이터 3개를 `reference/` 폴더로 이동, `.gitignore`에 추가해 git 추적 해제 (파일 자체는 로컬에 유지)
- 이동으로 깨지는 문서 내 경로 참조 수정 (`dev-request/CLAUDE.md`, `product-spec/CLAUDE.md`)

**문서 고도화**
- 루트 `CLAUDE.md`: 폴더 구조·핵심 기능 표가 실제 코드 상태를 반영 못 하고 있어(제안서·리뷰분석·문서수정·관리자·Notion 불러오기 등이 문서에 없었음) 전면 갱신. 스트리밍/프롬프트 캐싱 원칙을 "개발 원칙" 및 "Claude API 활용 방식"에 새 항목으로 추가
- `plan.md`: "Notion 연동 미개발"이 사실은 불러오기는 이미 구현되어 있고 저장만 미개발인 상태였음 — 정확히 구분해서 갱신. 이미 해결된 결정 보류 사항(PPT 내보내기, Vercel 배포) 정리
- `progress.md`: 2026-05-13~05-29 초기 개발 이력을 하나의 요약 블록으로 압축 (아래 참고), 누락되어 있던 06-18 작업 보완 기록

### 현재 상태
- 전체 기능 정상 동작, 실제 API 호출로 page-flow·final-plan 스트리밍 동작 확인 완료
- 문서(CLAUDE.md/plan.md/progress.md) 최신 코드 상태와 일치

### 다음 작업
- 상세 페이지 플로우 섹션별 병렬 생성 검토 (실제 생성 시간 단축, plan.md 참고)
- Notion 저장 연동 논의

### 특이사항 / 결정 사항
- 스트리밍 규칙 확정: 출력이 큰 기능은 반드시 화면까지 실시간 스트리밍 (CLAUDE.md "응답 속도" 항목 참고)
- 참고자료성 파일은 `reference/` 폴더에 모아 git에서 제외하는 것으로 정리 방식 확정 — 앞으로 추가되는 샘플·원본 데이터도 이 폴더에 보관

---

## 2026-06-18

### 완료한 작업
- 개발의뢰서 설명란에 '기타 참고사항' 항목 추가, 토글 UI로 개선, 카드 스타일 적용
- 수출 스크리닝 국가 옵션에 GCC(중동) 추가

### 현재 상태
- 개발의뢰서 기능 안정화 계속 진행 중

### 다음 작업
- Phase 3 Notion 연동 논의 (이후 07-02에 불러오기는 이미 구현되어 있었음을 재확인)

---

## 2026-06-15

### 완료한 작업
- 리뷰 분석에 큐텐JP 지원 추가
  - `ReviewSource` 타입에 `qutenjp` 추가
  - 컬럼 매핑: 별점 인덱스 2, 리뷰내용 인덱스 3 (xlsx)
  - 일본어 리뷰 → 한국어 번역·분석 지원 (번역 지침 프롬프트 적용)
  - UI 버튼 추가 (올리브영 / 아마존 / 큐텐JP)

### 현재 상태
- 올리브영·아마존·큐텐JP 세 플랫폼 지원

### 다음 작업
- Phase 3 Notion 연동 논의

---

## 2026-06-08

### 완료한 작업

**리뷰 분석 기능 신규 개발**

- `web/app/(document-automation)/review-analysis/page.tsx` — 리뷰 분석 페이지 신설
- `web/app/api/review-analysis/route.ts` — 리뷰 파일 파싱 + Claude API 분석 라우트
- `web/lib/prompts/review-analysis.ts` — 5개 섹션 분석 프롬프트 (별점 분포·장단점·미충족 니즈·신제품 인사이트)
- 올리브영 xlsx / 아마존 CSV 두 가지 포맷 지원 (플랫폼별 컬럼 매핑)
- 아마존 영어 리뷰 → 한국어 분석 출력
- 대시보드 최상단에 리뷰 분석 카드 배치

### 현재 상태
- 리뷰 분석 기능 완료 및 배포
- 올리브영·아마존 두 플랫폼 모두 테스트 완료

### 다음 작업
- Phase 3 Notion 연동 논의

### 특이사항 / 결정 사항
- max_tokens 16000으로 설정 (8000에서 증가) — 82개 리뷰 분석 시 뒷 섹션 잘림 문제 해결
- 아마존은 CSV 형식, 올리브영은 xlsx 형식으로 파일 타입 자동 분기 처리

---

## 2026-06-01 (4차)

### 완료한 작업

**제안서 임시 접근 코드 발급 기능 추가**

- `/admin` 관리자 페이지 신설: 별도 비밀번호(`ADMIN_PASSWORD`) 인증 후 오늘의 6자리 임시코드 표시 및 복사
- `web/lib/temp-code.ts`: 비밀키 + KST 날짜 → SHA-256 해시 → 6자리 숫자, 매일 자정 자동 갱신
- `/api/admin/temp-code` API: 오늘의 임시코드 반환
- `/api/auth/admin` API: 관리자 비밀번호 인증
- `/api/auth/proposal` 수정: 기존 고정 비밀번호 외 임시코드도 허용
- 메인 대시보드 유틸리티 섹션에 관리자 카드 추가
- Vercel 환경변수 추가: `TEMP_PASSWORD_SECRET`, `ADMIN_PASSWORD`

### 현재 상태
- Phase 1·2 완료 + 문서 수정 + 관리자 임시코드 기능 완료
- 모든 기능 정상 동작 확인

### 다음 작업
- Phase 3 Notion 연동 논의

### 특이사항 / 결정 사항
- 임시코드 방식: 날짜+비밀키 SHA-256 해시 → DB 없이 서버리스 환경에서 동작
- 관리자 페이지는 기존 앱 비밀번호(2359) + 관리자 비밀번호 2중 인증

---

## 2026-06-01 (3차)

### 완료한 작업

**문서 수정 기능 신규 개발 + 편집 버그 수정 (테스트 확인)**

- `/doc-edit` 페이지 생성: 저장된 HTML 파일 첨부 → 텍스트 직접 수정 → HTML 저장
- 드래그 앤 드롭 / 파일 선택 모두 지원
- 모든 문서 유형 지원: 제안서, 개발의뢰서, 최종 기획안, 상세 페이지 플로우
- 메인 대시보드에 '유틸리티' 섹션으로 분리하여 카드 추가
- 편집 방식: 스크립트 주입 없이 부모 프레임에서 직접 `iframe.contentDocument` DOM 조작
- 버그 수정 3차: `requestAnimationFrame` → `setTimeout(500)` 교체
  - 상세페이지 HTML(870줄 CSS)은 파싱이 느려 raf이 빈 문서에 실행되는 문제
  - data-edit-init 대신 getElementById 가드로 변경
- 최종 결과: 개발의뢰서·최종기획안·상세페이지 모두 텍스트 편집 정상 동작 ✅

### 현재 상태
- Phase 1·2 완료 + 문서 수정 기능 추가 완료
- 모든 기능 정상 동작 확인

### 다음 작업
- Phase 3 Notion 연동 논의

### 특이사항 / 결정 사항
- srcdoc iframe 편집 주입 타이밍: requestAnimationFrame으로는 대용량 HTML 파싱 완료를 보장할 수 없음 → 항상 setTimeout(500) 사용 (error.md ERR-013 추가)

---

## 2026-06-01 (2차)

### 완료한 작업

**상세 페이지 플로우 — 디자인 사라짐 버그 수정 (테스트 확인)**

버그 1 (주원인): `parseHtmlToSections` 스타일 수집 순서 문제
- AI가 단일 래퍼 div로 섹션을 감쌌을 때 언래핑 과정에서 내부 `<style>` 태그를 버림
- head 구성이 style 수집보다 먼저 일어나는 순서 문제 → CSS 통째로 손실
- 수정: 스타일 전체 수집(wrapper 직접자식 + 래퍼 내부) → 언래핑 → head 구성 순으로 재구성

버그 2 (부원인): generic 반복 요소 탐지 범위 과도
- `<section class="s">` 같은 레이아웃 섹션에도 탐지가 걸려 `.detail-wrap`에 불필요한 버튼 추가
- 수정: `.detail-wrap` 직접 자식은 generic 탐지에서 제외

### 현재 상태
- 상세 페이지 플로우: 섹션 구성 정상 ✅ / 디자인 유지 ✅ / 텍스트 편집 ✅ / 요소 삭제·추가 ✅
- 사용자 테스트 완료 확인

### 다음 작업
- Phase 3 Notion 연동 논의

### 특이사항 / 결정 사항
- parseHtmlToSections에서 스타일 수집은 반드시 head 구성보다 먼저 실행해야 함 (error.md ERR-011 추가)

---

## 2026-06-01

### 완료한 작업

**최종 기획안 — JSON 파싱 오류 수정**
- 원인: `design_html` 필드를 JSON 안에 HTML로 담으면 쌍따옴표가 JSON을 깨뜨림
- 해결: `---HTML---` 구분자로 JSON과 HTML 분리 출력하도록 프롬프트 + 파싱 로직 변경
- `final-plan/route.ts`, `lib/prompts/final-plan.ts` 수정

**상세 페이지 플로우 — 섹션 파싱 오류 수정**
- 원인 A: AI가 `<div class="detail-wrap">` 래퍼를 추가 생성 → 섹션이 1개로 뭉침
- 원인 B: `<style>` 태그가 섹션으로 카운트됨 → 목록에 의미없는 항목 표시
- 원인 C: 단일 래퍼 div 안에 섹션이 묶임 → 파싱 실패
- 해결: route.ts에서 `detail-wrap` 래퍼 자동 제거 + parseHtmlToSections에서 style/script 건너뜀 + 단일 래퍼 div 언래핑

**상세 페이지 플로우 — 편집 기능 수정**
- 원인: CSS `:hover`가 iframe 안에서 신뢰성 없음 → 삭제/추가 버튼이 나타나지 않음
- 해결: CSS `:hover` → JS `mouseenter`/`mouseleave` 이벤트로 교체
- 레퍼런스 모드 대응: 같은 태그+클래스 반복 요소도 자동으로 삭제/추가 버튼 생성
- 섹션 목록 클릭 → 미리보기에서 해당 섹션으로 스크롤 + 파란 하이라이트 추가

**문서 관리 체계 수립**
- CLAUDE.md 전면 개편: 오류 방지 원칙, iframe 편집 원칙, JSON+HTML 분리 원칙 추가
- error.md 신규 생성: 오류 10개 기록 (ERR-001 ~ ERR-010)
- plan.md 신규 생성: 현재 상태, Phase 3 계획, 보류 사항 정리
- education.md 신규 생성: AI 활용 가이드 (효과적인 소통 방법)

### 현재 상태
- Phase 1·2 완료, Vercel 배포 중
- 문서 관리 체계: CLAUDE.md / progress.md / error.md / plan.md / education.md 5개 파일 운영

### 다음 작업
- 상세 페이지 플로우 재테스트, Phase 3 Notion 연동 논의

### 특이사항 / 결정 사항
- iframe 안 인터랙션은 CSS 의사 클래스 대신 항상 JS 이벤트로 처리 (CLAUDE.md에 원칙 추가)
- AI에게 JSON 안에 HTML 포함 요청 금지 (CLAUDE.md에 원칙 추가)
- 문서 파일 역할 분리: CLAUDE.md(규칙) / progress.md(이력) / error.md(오류) / plan.md(계획) / education.md(소통 가이드)

---

## 2026-05-13 ~ 2026-05-29 (초기 개발 — 요약)

Phase 1(문서 자동화 4종: 개발의뢰서·제품사양서·최종기획안·상세페이지플로우) + Phase 2(제안서 작성) 전체 완료, GitHub 연동, Vercel 배포까지 진행된 시기. 세부 일별 기록은 압축하고 흐름과 핵심 결정만 남김.

- **05-13~05-15**: 개발의뢰서(4개 제품유형) 완성, 노션 불러오기 최초 구현, 최종 기획안 PPT 방식으로 착수, 제품 사양서 완성(xlsx-populate로 원본 서식 보존)
- **05-16~05-17**: 제안서 작성 기능 신설(최초 6단계 프로세스 → 이후 로직발굴/마케팅방향성/제안서작성 3단계 UI로 분리), IP당 rate-limit 도입, 다중 사용자 배포 대비 점검(proxy.ts 미들웨어 이름 충돌 수정)
- **05-19~05-20**: 최종 기획안을 PPT 방식에서 HTML 방식으로 전환(이미지·경쟁사 분석 등 수동 작업이 많아 완전 자동화 한계로 판단), 하이브리드 생성 방식 도입(고정 섹션은 코드로 조립 + AI는 일부 섹션만 생성 → 속도 개선), iframe 직접 편집(designMode) 기능 추가
- **05-26~05-27**: 상세 페이지 플로우 신규 개발, AKKBELL CSS 컴포넌트 라이브러리를 코드에 내장(Claude는 카피만 생성), 레퍼런스 입력을 URL → 이미지/PDF 업로드로 전환(이미지 기반 상세페이지 대응), Phase 1 전체 완료
- **05-29**: GitHub 저장소 생성 및 최초 push, 노션 URL 검증 버그 수정

**지금도 유효한 핵심 결정:**
- xlsx 내보내기는 SheetJS 대신 xlsx-populate 사용 — 스타일(서식) 보존 목적
- 모델 분리: 출력량 적은 기능은 `MODEL_STANDARD`, 큰 출력은 `MODEL_LARGE` — `lib/constants.ts`에서 한 곳 관리
- Vercel 무료 플랜 파일 업로드 제한은 클라이언트 압축으로 대응 (서버 설정으로 우회 불가)
- 문서 관리 체계(CLAUDE.md/progress.md/error.md/plan.md/education.md 5개 파일 역할 분리)는 06-01에 수립
